# 工作清单

## 项目背景，约束和说明

* 参见[project.md](project.md)
* 目前我们的项目全面使用Python进行编程。
* 当前阶段不做流固耦合，只追求把固体仿真做好。
* 但是必须保证程序本身的通用性。
* **在进入下一个步骤之前，你必须保证前一个步骤的全部任务得到完成。**

## 运行环境

```bash
conda activate numerical
```
这个conda环境中已经安装好了基础的编译器和常用库，如果有其他需要，可在里面安装。

## 当前任务

* 目前我们的代码库中2.5D和3D混用，考虑到实际芯片封装的场景，彻底贯彻2.5D的思想进行建模或许更加有利。请你彻底改写我们的配置方式和代码处理系统，使其原生是2.5D的（也就是每层的版图设计用2D的形式来表达,，并把版图设计拆成独立、可组合的文件，在最终求解和网格化时再嵌入到3D模型）。请给我重构后所有需要修改的代码的完整版本。