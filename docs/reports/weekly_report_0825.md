# weekly\_report\_0825

## 实验和优化

- 为第三类边界条件分配额外自由度，在保持准确性同时获得边界条件的仿射性。
- 使用 Ruge Stuben AMG 替代朴素 CG，大幅度加快收敛。
- 确认容差的实际意义，同一个相对容差同时控制频移采样点数量和残差上限。
- 按照论文表述，稳态响应的温升相对误差被容差线性控制，而瞬态冲激响应的温升相对误差基本上是被容差的平方根控制，收敛相对慢（这和我们实验中观察到的情况基本上相符）。
- 残差的语义，可能严重影响相同参数选择下的阶数。论文中没有澄清，可能有以下几种：
    - **$L^2$ 范数**：$\eta = \|\mathbf{r}\|_2$；
    - **加权能量范数**：$\eta = \sqrt{\mathbf{r}^T \mathbf{Ar}}$
    - **相对残差**: $\eta =\frac{\|\mathbf{r}\|_2}{\|\mathbf{b}\|_2}$
- 尝试后得到结论，Flotherm 中的语义比较接近相对残差（在以此作为依据的时候，可以得到和 Flotherm 的阶数更接近的结果），但是仍不完全相同，取相对残差语义时，我们在相同容差取值下的阶数和实际精度仍然稍微少于 Flotherm，不过个位数差异，无伤大雅。
- 提取性能对标，两组仿射参数，范围均为 1 到 1e4，容差均为 1e-3：
    - 12 万 DOF：Flotherm ~ 78s，Ours ~ 66s
    - 43 万 DOF：Flotherm ~ 430s，Ours ~ 300s

## 后续工作

- 不再把边界温度也作为降阶变量，而是保留边界温度作为独立节点。
- 引入交界面细分和面积权重矩阵 ，让不同网格的边界节点自动“对齐”到一个公共接口上。这个处理可以直接在模型定义层面完成，无需知道原始网格细节。
- Connecting MOR-based boundary condition independent compact thermal models
