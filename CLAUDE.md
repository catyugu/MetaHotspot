# MetaHotspot

## 开发目标功能边界

- 参考 `legacy` 中的代码进行迁移开发，实现新的更简洁、高效、和来自另一款软件的xml格式兼容的新的求解器。
- 支持稳态、瞬态热方程求解
- 支持对流边界，恒温边界，绝热边界（默认），块热源等。

## 总体技术选型

- IO与接口：Python
- 计算核心：当前使用Python+Numba JIT，后续拟迁移到C/C++

## 当前建模配置方案

- 采取xml格式配置2.5D模型。
- Python程序接口接受且只接受一个主配置文件，用于完成整个仿真流程。
- 输出格式：vtu文件（可用Paraview等软件打开）+ 写入结果后的xml文件（输出到新文件里，避免覆盖原有文件内容）

## 数值技术

- 热模型：CTM（集总热模型），构建一维热阻热容网络（离散方案本质类似FVM/FDM）
- 网格方案：非共形网格。参考 `legacy/metahotspot` 中的处理方式。开发第一阶段中，依据xml文件中的 `<b:XArray>`, `<b:YArray>`, `<b:ZArray>` 来切分。
- 时间步进方法：目前用自适应步长（绝对容差配合相对容差）BDF2（如有必要，后续可以改用误差更小的方法）。
- 非线性：暂未考虑非线性。

## 代码规范

- 面向数据设计，禁止面向对象的继承，多态等。总是使用SoA而非AoS。
- 所有数据类用 `@dataclass` 标记。所有类用 `__slots__` 严格限制其成员。
- 必须使用规范单例日志输出，禁止使用原始的print。
- 所有函数、变量等必须有类型标注。

## 供参考验证的案例

- [案例一](examples/example1/example1.xml)
